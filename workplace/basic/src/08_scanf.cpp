#include <bits/stdc++.h>

int main()
{
    char s1[256];
    int i;

    scanf("%s", s1);
    printf("%s", s1);

    scanf("%i", &i);
    printf("%i", i);
}

/* Points
1. scanf(type, address)
   Second argument needs address, so type of int has to use &.
*/
